{
  "name": "1_cloud-configs",
  "description": "The derive job, and only the derive job: reads src/inputs/ + a_solutions/*/build.json + config.json + vault, writes every build-{container}.json and fleet JSON to 1_cloud-configs/dist/. Split out of 1_configs 2026-08-10 so that module holds only repo-universal config (git, GHA, dotfiles).",
  "build": {
    "type": "script-only",
    "entry": "build.sh"
  },
  "deploy": {
    "host": "local",
    "note": "Outputs stay in dist/; consumers read via symlinks from a_solutions/*/src/build-{container}.json."
  }
}
