{
  "_doc": "Ordered list of TS derivers executed by 9_others/build.sh derive. Runs after consolidate; order matters when a later deriver consumes an earlier one's output. Adding a deriver = append a JSON entry; build.sh remains untouched (fire-rule 6: data-driven, never hardcoded).",
  "_schema": "derivers/v1",
  "derivers": [
    {
      "name": "config-derive",
      "script": "src/derive/cloud-data-config-derive.ts",
      "description": "Per-consumer JSON files (build-*.json + cloud-data-*.json archive) from _cloud-data-consolidated.json"
    },
    {
      "name": "mesh-snapshot",
      "script": "src/derive/derive-mesh-snapshot.ts",
      "description": "WireGuard mesh + wstunnel TCP/443 fallback snapshot consumed by bb-net_wireguard-mesh panel as PORTAL_DATA[mesh]"
    },
    {
      "name": "build-workflows",
      "script": "src/derive/derive-build-workflows.ts",
      "description": "Runner-image registry (runners + probe + dispatch) extracted from 9_others/build.json. Replaces legacy cloud-data-runners.json. Consumed by 1_cicd/src/scripts/cloud-ship-* and tests via symlink 9_others/src/build-workflows.json -> ../../1_cloud-configs/dist/build-workflows.json"
    },
    {
      "name": "code-signatures",
      "script": "src/derive/derive-code-signatures.ts",
      "description": "Per-file code signatures (exports/imports/symbols, no bodies) for the cloud repo \u2192 dist/code-signatures-cloud.json. Deterministic structure layer feeding the kg-graph code nodes/edges AND the Opus semantic GraphRAG pass (~244k tokens vs 5.6M full source)."
    },
    {
      "name": "mcp-json",
      "script": "src/derive/derive-mcp-json.ts",
      "doc": "Project-scoped MCP server registry -> dist/mcp.json, copied into every repo as .mcp.json. Replaces cloud's tracked symlink to /home/diego/.mcp.json, which dangled everywhere but one laptop."
    },
    {
      "name": "dagu-repo-map",
      "script": "src/derive/derive-dagu-repo-map.ts",
      "doc": "Stamps ops_git-sync.yaml REPO_MAP from cloud-cgc-mcp build.json .runtime.octocode.repo_map. That DAG is the single writer of the octocode_repos volume; its hand-copied map went stale across the cloud-* rename (cloud:cloud cloned the symlink index repo, not cloud-infra; front-data was missing). Also fails the build if an index_repos entry has no repo_map entry, which would abort cloud-cgc-db-update.sh at :388."
    }
  ]
}
