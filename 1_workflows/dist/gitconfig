# Repo-level git config — deployed by build.sh workflow
# Ensures submodules always sync on push/pull for all contributors + CI
[push]
	recurseSubmodules = on-demand
[submodule]
	recurse = true
[fetch]
	recurseSubmodules = on-demand
[core]
	hooksPath = 1_workflows/dist/hooks
[pull]
	rebase = true
[rebase]
	autoStash = true
[alias]
	sync = !git fetch origin && git reset --hard origin/main && git submodule update --init --recursive --remote --rebase
