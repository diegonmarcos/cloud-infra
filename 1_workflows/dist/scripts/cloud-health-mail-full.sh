#!/usr/bin/env bash
# ┌──────────────────────────────────────────────────────────────┐
# │  DO NOT EDIT THIS FILE!                                      │
# │                                                              │
# │  This file is READ-ONLY generated output.                    │
# │  It is declaratively managed by the workflow engine.         │
# │                                                              │
# │  Engine: cloud/build.sh workflow                             │
# │  Source: cloud/1_workflows/src/scripts/cloud-health-mail-full.sh                                  │
# │                                                              │
# │  To change: edit the source, run build.sh workflow, commit.  │
# └──────────────────────────────────────────────────────────────┘

# ── Full mail health diagnostic ──
# Usage: health-mail-full.sh
set -euo pipefail

REPO_ROOT="${GITHUB_WORKSPACE:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}"
cd "$REPO_ROOT/a_solutions/bc-obs_c3-infra-mcp/src"

# Install deps if needed (GHA already has them, Dagu/CLI may not)
[ -d node_modules ] || npm install --no-audit --no-fund 2>/dev/null || true

tsx mcp/tools/health_mail.ts
