name: Ship services

on:
  push:
    paths:
      - "a_solutions/container-nix/bb-sec_*/**"
      - ".github/workflows/ship.yml"
    branches: [main]
  workflow_dispatch:
    inputs:
      service:
        description: "Force ship a specific service (leave empty for auto-detect)"
        required: false
        type: string

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      caddy: ${{ steps.changes.outputs.caddy || steps.merge.outputs.caddy }}
      caddy-docker: ${{ steps.changes.outputs.caddy-docker || steps.merge.outputs.caddy-docker }}
      rust-api: ${{ steps.changes.outputs.rust-api || steps.merge.outputs.rust-api }}
      rust-api-docker: ${{ steps.changes.outputs.rust-api-docker || steps.merge.outputs.rust-api-docker }}
      go-api: ${{ steps.changes.outputs.go-api || steps.merge.outputs.go-api }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changes
        id: changes
        uses: dorny/paths-filter@v3
        if: github.event_name == 'push'
        with:
          filters: |
            caddy:
              - 'a_solutions/container-nix/bb-sec_caddy/src/**'
            caddy-docker:
              - 'a_solutions/container-nix/bb-sec_caddy/src/Dockerfile.caddy'
              - 'a_solutions/container-nix/bb-sec_caddy/src/introspect-proxy/**'
            rust-api:
              - 'a_solutions/container-nix/bb-sec_rust-api/src/**'
            rust-api-docker:
              - 'a_solutions/container-nix/bb-sec_rust-api/src/Dockerfile'
              - 'a_solutions/container-nix/bb-sec_rust-api/src/src/**'
            go-api:
              - 'a_solutions/container-nix/bb-sec_go-api/src/**'

      - name: Force service override
        if: github.event_name == 'workflow_dispatch'
        run: |
          svc="${{ inputs.service }}"
          if [ -z "$svc" ] || [ "$svc" = "caddy" ]; then
            echo "caddy=true" >> "$GITHUB_OUTPUT"
            echo "caddy-docker=true" >> "$GITHUB_OUTPUT"
          fi
          if [ -z "$svc" ] || [ "$svc" = "rust-api" ]; then
            echo "rust-api=true" >> "$GITHUB_OUTPUT"
            echo "rust-api-docker=true" >> "$GITHUB_OUTPUT"
          fi
          if [ -z "$svc" ] || [ "$svc" = "go-api" ]; then
            echo "go-api=true" >> "$GITHUB_OUTPUT"
          fi
        id: force

      - name: Merge outputs
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "caddy=${{ steps.force.outputs.caddy || 'false' }}" >> "$GITHUB_OUTPUT"
          echo "caddy-docker=${{ steps.force.outputs.caddy-docker || 'false' }}" >> "$GITHUB_OUTPUT"
          echo "rust-api=${{ steps.force.outputs.rust-api || 'false' }}" >> "$GITHUB_OUTPUT"
          echo "rust-api-docker=${{ steps.force.outputs.rust-api-docker || 'false' }}" >> "$GITHUB_OUTPUT"
          echo "go-api=${{ steps.force.outputs.go-api || 'false' }}" >> "$GITHUB_OUTPUT"
        id: merge

  ship-caddy:
    needs: detect
    if: needs.detect.outputs.caddy == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-24.11

      - name: Set up Docker Buildx
        if: needs.detect.outputs.caddy-docker == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: needs.detect.outputs.caddy-docker == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.GCP_PROXY_SSH_KEY }}" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          cat >> ~/.ssh/config <<EOF
          Host gcp-proxy
            HostName ${{ secrets.GCP_PROXY_HOST }}
            User ${{ secrets.GCP_PROXY_USER }}
            IdentityFile ~/.ssh/id_deploy
            StrictHostKeyChecking no
          EOF

      - name: Setup SOPS age key
        run: |
          mkdir -p ~/.config/sops/age
          echo "${{ secrets.SOPS_AGE_KEY }}" > ~/.config/sops/age/keys.txt
          echo "SOPS_AGE_KEY_FILE=$HOME/.config/sops/age/keys.txt" >> "$GITHUB_ENV"

      - name: Install sops
        run: nix profile install nixpkgs#sops

      - name: Ship caddy
        run: |
          if [ "${{ needs.detect.outputs.caddy-docker }}" = "true" ]; then
            bash a_solutions/container-nix/bb-sec_caddy/build.sh ship
          else
            bash a_solutions/container-nix/bb-sec_caddy/build.sh redeploy
          fi

  ship-rust-api:
    needs: detect
    if: needs.detect.outputs.rust-api == 'true'
    runs-on: ubuntu-24.04-arm64
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-24.11

      - name: Set up Docker Buildx
        if: needs.detect.outputs.rust-api-docker == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: needs.detect.outputs.rust-api-docker == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.OCI_APPS_SSH_KEY }}" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          cat >> ~/.ssh/config <<EOF
          Host oci-apps
            HostName ${{ secrets.OCI_APPS_HOST }}
            User ${{ secrets.OCI_APPS_USER }}
            IdentityFile ~/.ssh/id_deploy
            StrictHostKeyChecking no
          EOF

      - name: Setup SOPS age key
        run: |
          mkdir -p ~/.config/sops/age
          echo "${{ secrets.SOPS_AGE_KEY }}" > ~/.config/sops/age/keys.txt
          echo "SOPS_AGE_KEY_FILE=$HOME/.config/sops/age/keys.txt" >> "$GITHUB_ENV"

      - name: Install sops
        run: nix profile install nixpkgs#sops

      - name: Ship rust-api
        run: |
          if [ "${{ needs.detect.outputs.rust-api-docker }}" = "true" ]; then
            bash a_solutions/container-nix/bb-sec_rust-api/build.sh ship
          else
            bash a_solutions/container-nix/bb-sec_rust-api/build.sh redeploy
          fi

  ship-go-api:
    needs: detect
    if: needs.detect.outputs.go-api == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-24.11

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.GCP_PROXY_SSH_KEY }}" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          cat >> ~/.ssh/config <<EOF
          Host gcp-proxy
            HostName ${{ secrets.GCP_PROXY_HOST }}
            User ${{ secrets.GCP_PROXY_USER }}
            IdentityFile ~/.ssh/id_deploy
            StrictHostKeyChecking no
          EOF

      - name: Setup SOPS age key
        run: |
          mkdir -p ~/.config/sops/age
          echo "${{ secrets.SOPS_AGE_KEY }}" > ~/.config/sops/age/keys.txt
          echo "SOPS_AGE_KEY_FILE=$HOME/.config/sops/age/keys.txt" >> "$GITHUB_ENV"

      - name: Install sops
        run: nix profile install nixpkgs#sops

      - name: Ship go-api
        run: bash a_solutions/container-nix/bb-sec_go-api/build.sh ship
