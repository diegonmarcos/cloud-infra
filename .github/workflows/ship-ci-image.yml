name: "Ship → CI image"

on:
  push:
    paths:
      - "III_unix"
      - "config.json"
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-image-build
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/diegonmarcos/cloud-builder-x-deb-nixhm

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Encode Dockerfile for label
        id: dockerfile
        run: echo "b64=$(base64 -w0 III_unix/cb_containers-builders/src/docker/Dockerfile)" >> "$GITHUB_OUTPUT"

      - name: Build and push cloud-builder image
        uses: docker/build-push-action@v6
        with:
          context: III_unix/cb_containers-builders/src
          file: III_unix/cb_containers-builders/src/docker/Dockerfile
          push: true
          build-args: |
            DOCKERFILE_B64=${{ steps.dockerfile.outputs.b64 }}
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}
