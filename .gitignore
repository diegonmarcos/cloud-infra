# ══════════════════════════════════════════════════════════
# REPO: cloud (PUBLIC)
# https://github.com/diegonmarcos/cloud
# ══════════════════════════════════════════════════════════

# ── Secrets & Credentials (reinforces global gitignore) ──
# KEEP IN SYNC WITH:
#   Pre-commit hook: workflows/src/hooks/pre-commit  (blocks even git add -f)
#   Global gitignore: ~/git/unix/ba_flakes_desktop/src/modules/programs/git.nix
#   Global gitignore: ~/git/unix/bb_flakes_termux/src/modules/programs/git.nix
## Decrypted secrets output
.secrets
*.secrets
**/.secrets
.secrets.d/
**/.secrets.d/
## Environment files
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example
## Private keys & certificates
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
## Auth tokens & credentials
*.token
*credentials*.json
*_token.json
*_tokens.json
## Cloud provider configs
.oci/
.gcloud/
## GPG & age
*.asc
*.age
!*.age.pub
## Terraform secrets
terraform.tfvars
*.tfvars
!*.tfvars.example

# ── Dependencies (managed by shared ~/.node_modules) ──
node_modules

# ── Repo-specific ──
## Terraform
.terraform/
## Nix build artifacts
result
.result
## Temporary files
*.tmp
## Generated data files → symlinks to cloud-data/ submodule (tracked)
## Obsidian
.obsidian/workspace-mobile.json

# Deployed secrets (never commit)
b_infra/home-manager/*/dist/.secrets
b_infra/home-manager/*/dist/.secrets.d/
