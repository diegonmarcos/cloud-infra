{
  "name": "cloud-data-config",
  "version": "1.0.0",
  "description": "Engines that generate cloud-data JSON hub from cloud/ sources. tsx ESM runtime.",
  "private": true,
  "type": "module",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.6.1"
  }
}
