# vm-pilot — delivery capsule for shared VM system modules
# Contains nix modules, scripts, data files — NOT a running container
# Consumers: nixhm-sudo-{vm} images extract /modules/ at build time
FROM scratch
COPY modules/ /modules/
