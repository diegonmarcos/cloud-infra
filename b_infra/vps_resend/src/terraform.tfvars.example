# Injected from src/secrets.yaml via _engine.sh step_secrets
# Placeholders ${VAR} are replaced with sops-decrypted values

resend_api_key       = "${RESEND_API_KEY}"
cloudflare_api_token = "${CLOUDFLARE_API_TOKEN}"
cloudflare_zone_id   = "${CLOUDFLARE_ZONE_ID}"
