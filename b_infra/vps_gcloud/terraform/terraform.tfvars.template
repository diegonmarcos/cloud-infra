# Google Cloud Configuration
# Authenticate: gcloud auth application-default login
# Project: diegonmarcos-infra-prod

project_id = "diegonmarcos-infra-prod"
region     = "us-central1"
zone       = "us-central1-a"

# SSH public key content (not a file path)
# Run: export TF_VAR_ssh_public_key=$(cat ~/git/vault/A0_keys/ssh/google_compute_engine.pub)
ssh_public_key = "FILL_FROM_VAULT"
