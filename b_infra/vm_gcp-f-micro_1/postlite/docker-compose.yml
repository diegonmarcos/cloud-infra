# SQLite REST API servers (ws4sqlite)
# These expose SQLite databases via REST API on the WireGuard interface
# Deployed on: gcp-f-micro_1 (35.226.147.64)
# Path on server: /home/diego/postlite/

services:
  sqlite-npm:
    image: germanorizzo/ws4sqlite:latest
    container_name: sqlite-npm
    restart: unless-stopped
    ports:
      - "10.0.0.1:8880:12321"
    volumes:
      - /home/diego/npm/data:/data
    command: ["-db", "/data/database.sqlite?mode=ro"]

  sqlite-vaultwarden:
    image: germanorizzo/ws4sqlite:latest
    container_name: sqlite-vaultwarden
    restart: unless-stopped
    ports:
      - "10.0.0.1:8881:12321"
    volumes:
      - /home/diego/vaultwarden/data:/data
    command: ["-db", "/data/db.sqlite3?mode=ro"]

  sqlite-ntfy:
    image: germanorizzo/ws4sqlite:latest
    container_name: sqlite-ntfy
    restart: unless-stopped
    ports:
      - "10.0.0.1:8882:12321"
    volumes:
      - /home/diego/ntfy/cache:/data
    command: ["-db", "/data/cache.db?mode=ro"]

  sqlite-authelia:
    image: germanorizzo/ws4sqlite:latest
    container_name: sqlite-authelia
    restart: unless-stopped
    ports:
      - "10.0.0.1:8883:12321"
    volumes:
      - /home/diego/authelia/config:/data
    command: ["-db", "/data/db.sqlite3?mode=ro"]

  # PostgreSQL wire protocol for SQLite (postlite)
  # Allows SQL clients like NocoDB to connect to SQLite via PostgreSQL protocol
  postlite-npm:
    build:
      context: .
      dockerfile: Dockerfile
    image: postlite:latest
    container_name: postlite-npm
    restart: unless-stopped
    ports:
      - "10.0.0.1:5433:5432"
    volumes:
      - /home/diego/npm/data:/data
    command: ["-addr", ":5432", "-data-dir", "/data"]

  postlite-vaultwarden:
    build:
      context: .
      dockerfile: Dockerfile
    image: postlite:latest
    container_name: postlite-vaultwarden
    restart: unless-stopped
    ports:
      - "10.0.0.1:5434:5432"
    volumes:
      - /home/diego/vaultwarden/data:/data
    command: ["-addr", ":5432", "-data-dir", "/data"]

  postlite-ntfy:
    build:
      context: .
      dockerfile: Dockerfile
    image: postlite:latest
    container_name: postlite-ntfy
    restart: unless-stopped
    ports:
      - "10.0.0.1:5435:5432"
    volumes:
      - /home/diego/ntfy/cache:/data
    command: ["-addr", ":5432", "-data-dir", "/data"]

  postlite-authelia:
    build:
      context: .
      dockerfile: Dockerfile
    image: postlite:latest
    container_name: postlite-authelia
    restart: unless-stopped
    ports:
      - "10.0.0.1:5436:5432"
    volumes:
      - /home/diego/authelia/config:/data
    command: ["-addr", ":5432", "-data-dir", "/data"]
