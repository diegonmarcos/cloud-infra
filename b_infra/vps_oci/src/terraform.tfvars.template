# OCI Terraform — secrets only
# Non-secret values live in terraform.json
# Get these from OCI Console or ~/.oci/config

tenancy_ocid     = "ocid1.tenancy.oc1..CHANGE_ME"
compartment_ocid = "ocid1.compartment.oc1..CHANGE_ME"
