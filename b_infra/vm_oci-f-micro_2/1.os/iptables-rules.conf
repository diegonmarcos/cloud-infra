# Micro2 oci-f-micro_2 iptables
# INPUT: ACCEPT (no custom firewall rules beyond Docker)
# OCI Security List is the primary perimeter
# Docker manages FORWARD/NAT chains
#
# Notable: net.ipv4.ip_forward=1 in sysctl (needed for Docker + WireGuard)
#
# Container port exposure:
# - Matomo hybrid: 8080
# - Windmill: 127.0.0.1:8000 (localhost only)
