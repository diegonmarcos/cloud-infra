{
  "_doc": "Top-level a_solutions dirs that are not container services and ship by git pull of the shared tree, not by an image. A dispatch whose every changed path is under one of these is a proven no-op (verdict=nothing-to-ship), not verdict=empty.",
  "dirs": ["_dispatch"]
}
